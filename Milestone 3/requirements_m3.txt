# requirements_m3.txt: Milestone 3 Dependencies
#
# Install with:
#   pip install -r requirements_m3.txt
#
# Python 3.9+ required (3.11 / 3.12 recommended, matching M1/M2).

# same from Milestones 1 and 2
ray[default]>=2.9.0        # Task-based parallelism + Actor model
networkx>=3.2.0            # Graph data structures
numpy>=1.26.0              # Vectorised rank-vector math
scipy>=1.11.0              # Sparse matrix ops (NetworkX backend)
requests>=2.31.0           # HTTP for CommonCrawl
warcio>=1.7.4              # WARC archive parsing
beautifulsoup4>=4.12.0     # HTML parsing for incremental crawl
lxml>=5.1.0                # Fast HTML parser backend
psutil>=5.9.0              # Per-worker peak-memory measurement

# New for Milestone 3
matplotlib>=3.8.0          # All evaluation figures
pandas>=2.1.0              # Aggregating metrics across runs for plotting
tqdm>=4.66.0               # Progress bars during long evaluation runs
